// 定义接口
export interface successItem {
  dirUrl: string;
  fileSize: number;
  dirName: string;
  author: string;
  preview?: string;
}
export interface baseItem {
  dirUrl: string;
  fileSize: number;
  dirName: string;
  author: string;
  preview?: string;
}
