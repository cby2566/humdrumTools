export interface successItem {
  dirUrl: string;
  fileSize: number;
  dirName: string;
  author: string;
}
export interface baseItem {
  dirUrl: string;
  fileSize: number;
  dirName: string;
  author: string
}
